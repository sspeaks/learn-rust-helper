## Hint 3: Algorithm Outline

```
function total_harvest(cycles):
    Step 1: Initialize a total variable to 0
    Step 2: For each cycle number in the range:
        Add that cycle number to the total
    Step 3: Return the total
```

**Note:** Use a `for` loop and a mutable accumulator. The loop variable receives each cycle number; add it to your running sum.

**Spoiler threshold:** High—clear structure, but not the Rust syntax.
