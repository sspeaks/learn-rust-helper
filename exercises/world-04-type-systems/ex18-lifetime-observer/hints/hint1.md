## Hint 1: Conceptual Question

Ask: "Where does the returned `&str` come from?" If a function returns a borrowed slice, the output lifetime must be tied to input lifetimes that own the data.

**Spoiler threshold:** Low—focuses on ownership/lifetime relationships.
