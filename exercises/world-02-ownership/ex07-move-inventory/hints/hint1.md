## Hint 1: Conceptual Question

You have a vector of crates and need to remove one by ID and transfer ownership to the caller. How do you find an element in a vector? Once found, how do you remove it and return it as owned data? What happens to `Option` if the element isn't found?

**Spoiler threshold:** Low—guiding questions.
