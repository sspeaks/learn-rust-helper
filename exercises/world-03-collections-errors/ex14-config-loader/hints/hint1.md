## Hint 1: Conceptual Question

You're parsing structured data (key=value pairs) and need to detect various errors: missing fields, bad numbers, duplicates, unknown keys. How do you represent different error types in an enum? How do you bubble errors up and convert or propagate them?

**Spoiler threshold:** Low—asks about error design.
